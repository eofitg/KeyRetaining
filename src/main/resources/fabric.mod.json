{
  "schemaVersion": 1,
  "id": "keyretaining",
  "version": "${version}",
  "name": "KeyRetaining",
  "description": "Retain your key state right after closing an in game gui.",
  "authors": ["eofitg"],
  "contact": {},
  "license": "All-Rights-Reserved",
  "environment": "client",
  "entrypoints": {
    "main": [
      "net.eofitg.keyretaining.KeyRetaining"
    ]
  },
  "mixins": [
    "keyretaining.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}"
  }
}
